#include "main.h"
#include <stdio.h>

/**
 * _isupper - check for uppercase.
 *
 * Return: 1 or 0
 */
int _isupper(int i )
{
  
  if (i >= 'A' && i<='Z')
    {
            return(1)
    }
    return(0);
}
